import { StyleSheet } from "react-native";
export default StyleSheet.create({
    container: {
        flex:1,
        flexDirection: 'row',
        justifyContent: 'space-between',
        margin: 6,
        padding: 8,
        borderRadius: 4,
        borderColor: "black",
        borderWidth: 1,
    }
})
